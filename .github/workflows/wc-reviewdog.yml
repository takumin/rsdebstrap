---
name: Reviewdog
on:
  workflow_call:
permissions: {}
env:
  CARGO_INCREMENTAL: '0'
  RUSTC_WRAPPER: ${{ !startsWith(github.ref, 'refs/tags/') && 'sccache' || '' }}
  SCCACHE_CACHE_SIZE: '1G'
  # cargo runs the RUSTC_WRAPPER from each crate's source dir, where aqua can't
  # find .aqua/aqua.yaml by upward search; point it at the repo config so the
  # `sccache` command resolves regardless of the working directory.
  AQUA_GLOBAL_CONFIG: ${{ github.workspace }}/.aqua/aqua.yaml
concurrency:
  group: reviewdog-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}
jobs:
  ##############################################################################
  # Matrix
  ##############################################################################
  matrix:
    name: Matrix
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      targets: ${{ steps.matrix.outputs.result }}
    steps:
    - name: Checkout
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        persist-credentials: false
    - name: Setup aqua
      uses: aquaproj/aqua-installer@96a9bc20066c5bf5e275b41019cfc165b25f4e2e # v4.0.5
      with:
        aqua_version: v2.62.0
    - name: Generate Matrix
      id: matrix
      shell: bash
      run: echo "result=$(task reviewdog:matrix)" >> "$GITHUB_OUTPUT"
    - name: Check Matrix
      env:
        REVIEWDOG_MATRIX: ${{ steps.matrix.outputs.result }}
      run: |-
        echo "${REVIEWDOG_MATRIX}" | jq
  ##############################################################################
  # Runner
  ##############################################################################
  reviewdog:
    needs: matrix
    name: Reviewdog (${{ matrix.name }})
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.matrix.outputs.targets) }}
    permissions:
      actions: read
      checks: write
      contents: read
      pull-requests: write
    steps:
    - name: Checkout
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        persist-credentials: false
    - name: Setup aqua
      uses: aquaproj/aqua-installer@96a9bc20066c5bf5e275b41019cfc165b25f4e2e # v4.0.5
      with:
        aqua_version: v2.62.0
        aqua_opts: -t reviewdog,${{ matrix.target }}
    - name: Restore rustup toolchain cache
      if: matrix.name == 'clippy'
      uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        key: rustup-${{ hashFiles('rust-toolchain.toml') }}
        restore-keys: |-
          rustup-
        path: ~/.rustup/
    - name: Setup sccache
      if: matrix.name == 'clippy'
      run: task tool:aqua:tag:sccache
    - name: Restore sccache (deps)
      id: sccache
      if: ${{ matrix.name == 'clippy' && !startsWith(github.ref, 'refs/tags/') }}
      uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        key: sccache-clippy-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |-
          sccache-clippy-
        path: ~/.cache/sccache
    - name: Setup reporter
      id: reporter
      run: |-
        case "${{ github.event_name }}" in
        "push" )                echo "type=github-check" >> "$GITHUB_OUTPUT" ;;
        "pull_request" )        echo "type=github-pr-review" >> "$GITHUB_OUTPUT" ;;
        "pull_request_target" ) echo "type=github-pr-review" >> "$GITHUB_OUTPUT" ;;
        * )                     echo "type=local" >> "$GITHUB_OUTPUT" ;;
        esac
    - name: Running reviewdog
      run: task "${REVIEWDOG_TARGET}"
      env:
        REVIEWDOG_TARGET: ${{ matrix.target }}
        REVIEWDOG_REPORTER: ${{ steps.reporter.outputs.type }}
        REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        REVIEWDOG_ARGS: -log-level 'debug' -tee
    - name: sccache stats
      if: ${{ matrix.name == 'clippy' && !startsWith(github.ref, 'refs/tags/') }}
      run: sccache --show-stats
    - name: Save sccache (deps)
      if: matrix.name == 'clippy' && github.ref == 'refs/heads/main' && steps.sccache.outputs.cache-hit != 'true'
      uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        key: ${{ steps.sccache.outputs.cache-primary-key }}
        path: ~/.cache/sccache
