---
name: Build
on:
  workflow_call:
permissions: {}
concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}
jobs:
  matrix:
    name: Matrix
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      targets: ${{ steps.matrix.outputs.result }}
    steps:
    - name: Checkout
      uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      with:
        persist-credentials: false
        sparse-checkout: |-
          .aqua
          scripts
          src
          tasks
    - name: Setup aqua
      uses: aquaproj/aqua-installer@96a9bc20066c5bf5e275b41019cfc165b25f4e2e # v4.0.5
      with:
        aqua_version: v2.62.0
    - name: Generate Matrix
      id: matrix
      shell: bash
      run: echo "result=$(task build:matrix)" >> "$GITHUB_OUTPUT"
    - name: Check Matrix
      env:
        BUILD_MATRIX: ${{ steps.matrix.outputs.result }}
      run: |-
        echo "${BUILD_MATRIX}" | jq
  build:
    needs: matrix
    name: ${{ matrix.profile }}:${{ matrix.target }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.matrix.outputs.targets) }}
    steps:
    - name: Checkout
      uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      with:
        persist-credentials: false
    - name: Setup aqua
      uses: aquaproj/aqua-installer@96a9bc20066c5bf5e275b41019cfc165b25f4e2e # v4.0.5
      with:
        aqua_version: v2.62.0
    # Tag builds restore no caches at all: release artifacts are always built
    # from freshly fetched, verified-at-download inputs.
    - name: Restore cargo deps cache
      if: ${{ !startsWith(github.ref, 'refs/tags/') }}
      uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        key: deps-${{ hashFiles('**/Cargo.lock') }}
        path: ~/.cargo/registry/
        restore-keys: deps-
    - name: Restore setup build aqua cache
      if: ${{ !startsWith(github.ref, 'refs/tags/') }}
      uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        key: setup-build-aqua-${{ hashFiles('.aqua/**/*.yaml', '.aqua/**/*.yml') }}
        path: ~/.local/share/aquaproj-aqua/
        restore-keys: setup-build-aqua-
    - name: Restore rustup cache
      if: ${{ !startsWith(github.ref, 'refs/tags/') }}
      uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        key: rustup-${{ hashFiles('rust-toolchain.toml') }}
        path: ~/.rustup/toolchains/
    - name: Restore build cache
      if: ${{ !startsWith(github.ref, 'refs/tags/') }}
      id: build-cache-restore
      uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        key: build-${{ matrix.profile }}-${{ matrix.target }}-${{ hashFiles('rust-toolchain.toml') }}-${{ hashFiles('**/Cargo.lock') }}
        path: target/
        # No fallback on main: its saves must start from a clean slate so stale
        # artifacts never accrete into the shared cache.
        restore-keys: ${{ github.ref != 'refs/heads/main' && format('build-{0}-{1}-{2}-', matrix.profile, matrix.target, hashFiles('rust-toolchain.toml')) || '' }}
    - name: Setup build
      run: |-
        task setup:build
      env:
        BUILD_PROFILE: ${{ matrix.profile }}
        BUILD_TARGET: ${{ matrix.target }}
    - name: Build ${{ matrix.profile }} ${{ matrix.target }}
      run: |-
        task build:build
      env:
        CARGO_INCREMENTAL: '0'
        BUILD_PROFILE: ${{ matrix.profile }}
        BUILD_TARGET: ${{ matrix.target }}
    - name: Generate sha256sum
      run: |-
        task build:sha256sum
      env:
        BUILD_PROFILE: ${{ matrix.profile }}
        BUILD_TARGET: ${{ matrix.target }}
    - name: Verify sha256sum
      run: |-
        task build:sha256sum:verify
      env:
        BUILD_PROFILE: ${{ matrix.profile }}
        BUILD_TARGET: ${{ matrix.target }}
    - name: Generate cosign
      if: ${{ !startsWith(github.event_name, 'pull') && matrix.profile == 'release' }}
      run: |-
        task build:cosign
      env:
        BUILD_PROFILE: ${{ matrix.profile }}
        BUILD_TARGET: ${{ matrix.target }}
    - name: Generate artifact attestation
      if: ${{ !startsWith(github.event_name, 'pull') && matrix.profile == 'release' }}
      uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
      with:
        subject-name: rsdebstrap-release-${{ matrix.target }}
        subject-path: dist/rsdebstrap-release-${{ matrix.target }}
    - name: Upload artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: rsdebstrap-${{ matrix.profile }}-${{ matrix.target }}
        path: dist/
    - name: Save build cache
      if: github.ref == 'refs/heads/main' && steps.build-cache-restore.outputs.cache-hit != 'true'
      uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        key: ${{ steps.build-cache-restore.outputs.cache-primary-key }}
        path: target/
